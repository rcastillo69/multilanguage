// This is a single-line comment

const std = @import("std");

// The main function is the entry point of the Zig program
pub fn main() void {
    // Printing "Hello, World!" to the console
    std.debug.print("Hello, World!\n", .{});
}
